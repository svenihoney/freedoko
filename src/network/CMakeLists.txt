add_subdirectory(actor)
add_subdirectory(FreeDoko)
add_subdirectory(gnet)
set(LOCAL_SOURCES
  server.os.cpp
  listener.cpp
  actor.cpp
  server.cpp
  types.cpp
  connection.cpp
)
toglobalsources(${LOCAL_SOURCES})
